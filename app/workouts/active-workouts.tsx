import { Text, View } from "react-native";

export default function ActiveWorkouts() {
  return (
    <View className="flex-1 justify-center items-center">
      <Text className="text-3xl">Active Workouts</Text>
    </View>
  );
}
